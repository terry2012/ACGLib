package com.acg.lib.validation.state;

/**
 * Represents the state of a view.
 *
 * For now we just handle checked and unchecked, but later on we can extend this if we need to
 */
public enum ViewState {
    CHECKED, UNCHECKED;
}
